<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application>
        <service
            android:name="com.rubean.interviewGame.GameBotService"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="com.rubean.interviewGame.GameBotService.STARTORBIND"/>
            </intent-filter>
        </service>

    </application>

</manifest>